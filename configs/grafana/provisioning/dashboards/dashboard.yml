apiVersion: 1

providers:
  - name: "faas-dashboards"
    orgId: 1
    folder: "FaaS"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
